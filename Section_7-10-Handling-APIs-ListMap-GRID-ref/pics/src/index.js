import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// Index is used to render the App(main component) to the DOM/Browser
ReactDOM.render(
    <App />, 
    document.querySelector('#root')
);